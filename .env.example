# Environment Variables for ProxyPal NVIDIA Load Balancer

# Path to configuration file
CONFIG_PATH=config.yaml

# Server configuration (optional - can also be set in config.yaml)
# SERVER_PORT=8080
# SERVER_HOST=0.0.0.0

# Logging configuration (optional)
# LOG_LEVEL=info
